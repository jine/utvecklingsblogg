import { createAuthClient } from "better-auth/client";
// import { createAuthClient } from "better-auth/react" // from docs? make sure to import from better-auth/react

export const authClient = createAuthClient();
